export interface Commission {
    CommissionId: number;
    PolicyNo: number;
    AgentId: number;
    CustomerId: number;
    CommissionAmount: number;
    // IsActive: boolean; // Uncomment this line if needed
  }